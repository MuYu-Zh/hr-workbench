# 人事管理工作台 · 数据模型设计（IndexedDB 本机存储）

- 版本：v1.2（评审修订：P0 项全部解决——唯一性策略、Blob 备份、复合索引写回、审批枚举统一、离职状态链、日期规范、status_log 等）
- 存储决策：业务数据存 IndexedDB，轻量配置存 localStorage（已确认，见《需求单》5.1）
- 使用范围：个人单机使用（liqianyu）
- 评审依据：《数据模型评审报告》E/F/R/I/S 系列发现项

---

## 1. 存储架构总览

### 1.1 数据库定义
- 数据库名：`hr_workbench`
- 版本：`1`（升级时递增，走 `onupgradeneeded` 迁移，见第 7 节）
- ObjectStore 数量：49 个（见第 3 节清单）

### 1.2 localStorage 键（轻量配置）
| 键 | 内容 |
|---|---|
| `hr.settings` | 系统参数（键结构见下） |
| `hr.profile` | 个人资料（姓名、工号、联系方式）+ 密码锁配置 |
| `hr.ui.state` | 菜单展开状态、当前选中菜单、主题偏好 |
| `hr.data.version` | 数据备份/迁移版本标记 |

**`hr.settings` 键结构示例（E-19/F-08）**
| 分组 | 参数键 | 默认值 | 说明 |
|---|---|---|---|
| 提醒 | remindDays | {contract:[60,30], cert:30, birthday:7, probation:15} | 各提醒提前天数 |
| 考勤 | attendance | {lateThresholdMin:5, defaultStart:'09:00', defaultEnd:'18:00'} | 迟到阈值与默认班次 |
| 社保 | social | {pension:{company:16,personal:8}, medical:{...}, ...} | 各险种比例（%） |
| 公积金 | fund | {companyRate:12, personalRate:12} | 缴纳比例（%） |
| 绩效 | perf | {gradeRules:[{grade:'S',min:90},...], maxScore:100} | 等级分界 |
| 工资条 | payslip | {roundTo:0.01} | 金额精度 |
| 附件 | attachment | {maxSizeMB:20} | 单附件上限 |

### 1.3 通用字段约定（每个 objectStore 均有）
| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string (UUID) | 主键，全局唯一 |
| `createdAt` | number (ms) | 创建时间戳 |
| `updatedAt` | number (ms) | 最后修改时间戳 |
| `deleted` | number | 软删除标志：0=正常，1=已删除（默认 0） |
| `status` | string | 业务状态（各表独立枚举，见各表说明） |

> **软删除约定**：所有列表查询默认 `deleted=0`；删除操作置 1 而非物理删除，保证历史可追溯、可恢复。系统设置"数据清理"可物理清空。

### 1.4 ID 生成与唯一性策略
- 统一 UUID v4（浏览器 `crypto.randomUUID()`，无依赖、无并发冲突）。
- 业务编号（工号、合同编号、需求编号）为可编辑的业务字段，非主键。
- **唯一性校验（R-09）**：**不建 unique index**（IndexedDB unique index 会与软删除行冲突）；唯一性=写入前查询校验且**排除 deleted=1** 的记录，校验失败 UI 提示。
- **工号复用策略（D3）**：deleted=1 的工号允许复用于新员工；离职员工"恢复为在职"沿用原工号（复用校验自动跳过旧软删除行）。

### 1.5 日期存储规范（I-05，全库统一）
| 类别 | 存储格式 | 示例 | 适用范围 |
|---|---|---|---|
| date-only（仅日期） | string `'YYYY-MM-DD'` | `'2025-01-15'` | hireDate、birthDate、startDate、expireDate、month 等 |
| datetime（含时间） | number (ms 时间戳) | `1736899200000` | createdAt、submitTime、提醒时间等 |

> **原因**：`new Date('2025-01-15')` 按 UTC 解析，东八区读取会偏移一天，污染生日/到期/月度汇总。date-only 一律存字符串，比较用字典序；转换用 `new Date(y, m-1, d)` 本地构造。

### 1.6 必填与格式校验标注（F-08）
- 各表字段说明中的类型列追加约定：`必填` 前缀表示必填（如 `必填 string`），否则可选。
- 通用格式约束（录入时校验，UI 提示）：
  - 手机号：`^1\d{10}$`
  - 身份证：18 位（末位可为 X），展示脱敏（`110***********1234`）
  - 邮箱：标准格式
  - 日期：`YYYY-MM-DD`；金额：非负、两位小数
  - 工号/合同编号/证书编号：写入前逻辑唯一校验（排除 deleted）

### 1.7 附件存储与备份策略（I-01/R-11/S-10）
- 附件 Blob（证件扫描件、简历、合同、培训资料等）以 **Blob 值**存于 `attachment` 表。
- 单附件上限默认 20MB，可配置；图片支持前端压缩后再存（证件扫描建议 ≤5MB）。
- **备份导出方案（已定案）**：`JSON.stringify` 无法序列化 Blob，故导出采用**双段格式**：
  - 段一 `data.json`：全部业务数据（附件记录仅含元数据 fileName/mimeType/size/id）；
  - 段二 `attachments/`：附件按 id 导出为独立二进制文件（Blob → File 直接落盘），不转 base64（避免 33% 体积膨胀）；
  - 打包为 **ZIP**（含 data.json + attachments/ + `config.json` 配置段）；附件多时可分卷。
  - 恢复：ZIP 解包 → 校验版本 → 附件按 id 重新入 IndexedDB。
- **备份覆盖范围（I-03）**：`config.json` 含 hr.settings / hr.profile / hr.ui.state 序列化，恢复时二段写入（IndexedDB 数据 + localStorage 配置），保证备份完整。
- **附件清理（R-11）**：`attachment` 表**不设 ownerType/ownerId 冗余字段**（同一附件可能被多业务引用）；物理清理时扫描各业务表 attachmentId 引用，无引用的附件才删除。

---

## 2. 数据表清单总览

| # | 表名（store） | 模块 | 用途 |
|---|---|---|---|
| 1 | `department` | 组织架构 | 部门树 |
| 2 | `position` | 组织架构 | 岗位字典 |
| 3 | `grade` | 组织架构 | 职级字典（职级体系） |
| 4 | `headcount` | 组织架构 | 人员编制 |
| 5 | `employee` | 员工档案 | 员工主表（含在职/离职） |
| 6 | `employee_attachment` | 员工档案 | 员工证件/附件归档 |
| 7 | `recruit_requirement` | 招聘 | 招聘需求登记 |
| 8 | `candidate` | 招聘 | 候选人简历库 |
| 9 | `interview` | 招聘 | 面试记录（时间线） |
| 10 | `offer` | 招聘 | offer 发放记录 |
| 11 | `attendance_request` | 考勤 | 请假/加班/出差申请（统一审批） |
| 12 | `attendance_remedy` | 考勤 | 补卡申请 |
| 13 | `attendance_anomaly` | 考勤 | 考勤异常 |
| 14 | `shift` | 考勤 | 班次字典 |
| 15 | `schedule` | 考勤 | 排班明细 |
| 16 | `attendance_monthly` | 考勤 | 月度考勤汇总（固化） |
| 17 | `salary_account` | 薪资 | 员工薪资台账 |
| 18 | `payroll` | 薪资 | 月度工资表（主表） |
| 19 | `payroll_item` | 薪资 | 工资表明细（每员工每项） |
| 20 | `salary_adjust` | 薪资 | 薪资调整记录 |
| 21 | `deduction_detail` | 薪资 | 个税/社保代扣明细 |
| 22 | `payslip_issue` | 薪资 | 工资条发放记录 |
| 23 | `payslip_receipt` | 薪资 | 工资条员工级发放明细 |
| 23 | `perf_cycle` | 绩效 | 考核周期 |
| 24 | `perf_review` | 绩效 | 绩效考核表 |
| 25 | `perf_review_item` | 绩效 | 考核指标明细 |
| 26 | `perf_interview` | 绩效 | 绩效面谈记录 |
| 27 | `training_plan` | 培训 | 培训计划 |
| 28 | `training_record` | 培训 | 培训记录归档 |
| 29 | `skill_cert` | 培训 | 技能证书 |
| 30 | `training_eval` | 培训 | 培训效果评估 |
| 31 | `contract` | 合同 | 合同台账（含续签/变更） |
| 32 | `contract_template` | 合同 | 合同模板 |
| 33 | `probation` | 合同 | 试用期记录 |
| 34 | `social_insurance` | 社保公积金 | 社保缴纳台账 |
| 35 | `housing_fund` | 社保公积金 | 公积金缴纳台账 |
| 36 | `social_declare` | 社保公积金 | 增减员申报 |
| 37 | `social_base_adjust` | 社保公积金 | 缴费基数调整 |
| 38 | `benefit_claim` | 社保公积金 | 社保待遇申领 |
| 39 | `transfer_record` | 异动 | 员工异动统一记录（转正/调岗/调薪/晋升/降职/离职） |
| 40 | `resignation_handover` | 异动 | 离职交接清单 |
| 41 | `todo` | 待办 | 每日待办事项 |
| 42 | `memo` | 待办 | 备忘录笔记 |
| 43 | `reminder` | 待办 | 重要事项提醒 |
| 44 | `quick_link` | 常用网址 | 快捷链接 |
| 45 | `approval_record` | 通用 | 审批节点/留痕（支撑多级扩展） |
| 46 | `status_log` | 通用 | 全业务状态变更留痕（非审批类） |
| 47 | `attachment` | 通用 | 全系统统一附件库 |
| 48 | `sys_dict` | 通用 | 业务字典（渠道、请假类型、异常类型、证书类型、绩效等级等） |

> 派生数据（组织架构图、招聘渠道统计、绩效等级分布、考核结果汇总、在职人数、首页看板各卡片）由查询实时聚合生成，不落表。

---

## 3. 各表详细结构

> 约定：**PK**=主键索引；**IDX**=普通索引（用于筛选/排序）；字段类型按 IndexedDB 支持标注（string/number/boolean/object/Blob/Date）。
> 说明：表格中以 `a / b` 紧凑书写的一行表示两个独立字段；其中标注 IDX 的表示两者均为独立索引，标注（复合）的表示该行两字段组成一个复合索引（keyPath 数组）。

### 3.1 组织架构

#### `department` 部门
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| name | string | | 部门名称 |
| parentId | string | IDX | 上级部门 id（null=顶级） |
| manager | string | | 负责人（员工 id） |
| establishedDate | string YYYY-MM-DD | | 成立日期 |
| closedDate | string YYYY-MM-DD | | 撤销日期（撤销时写入） |
| status | string | IDX | normal / closed |
| sortOrder | number | | 同级排序 |

#### `position` 岗位字典
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| name | 必填 string | | 岗位名称 |
| departmentId | string | IDX | 所属部门（可空=通用岗位） |
| series | string | IDX | 序列：management / professional / functional / production |
| sortOrder | number | | 排序 |
| status | string | IDX | active / disabled（停用） |

> 岗位与职级**分离建模**（同一岗位可对应多个职级）：岗位表只管岗位本身，职级体系见下方 `grade` 表；`employee` 同时引用 positionId + gradeId。
> 引用完整性（R-03）：被 employee/headcount 引用的岗位/职级/部门禁止物理删除；软删除时提示挂靠员工数量；manager 变更时同步提示。

#### `grade` 职级字典（职级体系）
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| code | string | IDX | 职级编码，如 P3 / M1（唯一） |
| name | string | | 职级名称，如 高级工程师 |
| series | string | IDX | 序列：management / professional / functional / production |
| order | number | | 职级排序权重（晋升/降职判断用，越大越高） |
| salaryBand | object | | 薪资带宽 {min, max}（可选） |
| promotionPath | string | | 晋升路径说明（可选） |

#### `headcount` 人员编制
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| departmentId | string | IDX | |
| positionId | string | IDX | |
| quota | number | | 编制人数 |
| remark | string | | 备注 |
| effectiveFrom | string YYYY-MM-DD | | 生效日期 |

> 在职人数由 `employee` 实时统计，与 quota 对比生成超编预警（只读派生，不落表）。

### 3.2 员工档案

#### `employee` 员工主表
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeNo | 必填 string | IDX（逻辑唯一，排除 deleted） | 工号 |
| name | 必填 string | IDX | 姓名 |
| gender | string | | male / female |
| nationality | string | | 民族 |
| maritalStatus | string | | 婚姻状况：single / married / divorced / other |
| household | string | | 户籍 |
| birthDate | string YYYY-MM-DD | IDX | 出生日期（生日提醒用） |
| idCard | 必填 string | | 身份证号（18 位校验、展示脱敏，仅详情可见） |
| phone | 必填 string | | 手机（格式校验） |
| homePhone | string | | 家庭电话 |
| email | string | | 邮箱（格式校验） |
| address | string | | 现住址 |
| emergencyContact | object | | {name, phone, relation} |
| photoAttachmentId | string | | 照片附件 id |
| departmentId | string | IDX | 所属部门 |
| positionId | string | IDX | 岗位（引用 position） |
| gradeId | string | IDX | 职级（引用 grade，可空=未定级） |
| hireDate | string YYYY-MM-DD | IDX | 入职日期 |
| regularDate | string YYYY-MM-DD | | 转正日期 |
| employmentType | string | | fulltime / intern / parttime |
| education | string | | 学历 |
| school | string | | 毕业院校 |
| major | string | | 专业 |
| graduateDate | string YYYY-MM-DD | | 毕业时间 |
| bankCardNo | string | | 发薪银行卡号 |
| socialAccountNo | string | | 社保账号 |
| fundAccountNo | string | | 公积金账号 |
| status | string | IDX | active(在职) / resigned(离职) / resigning(离职交接中) |
| resignInfo | object | | 离职信息 {date, type, reason, handoverDone, handoverBy, handoverDate}（status=resigned 时） |
| resignDate | string YYYY-MM-DD | IDX | 离职日期独立字段（E-02/R-05，支持离职档案按时间检索） |
| remark | string | | 备注 |

> 员工删除 = 软删除；离职走状态链（E-18/S-07）：**resign 审批通过 → status=resigning → 交接 allDone=true → status=resigned + 写 resignInfo/resignDate**。花名册"离职"按钮统一发起 transfer_record(type=resign) 审批（E-03），不允许直接改状态。
> **试用期不占用 status**：是否试用期由 `probation` 表（status=running）判定，`regularDate` 为转正快照（为空=未转正）。花名册"转正状态"列 = regularDate 为空 ? 试用中 : 已转正；在职查询 = status=active。
> 简历/证件等附件经 `employee_attachment` 关联。

#### `employee_attachment` 员工证件/附件归档
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | 所属员工 |
| category | string | IDX | id_card / diploma / cert / contract / photo / other |
| title | string | | 附件名称 |
| attachmentId | string | IDX | 指向 `attachment` |
| expireDate | string YYYY-MM-DD | IDX | 有效期（证书类，到期提醒用） |
| remark | string | | 备注 |

### 3.3 招聘

#### `recruit_requirement` 招聘需求
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| reqNo | string | | 需求编号 |
| departmentId | string | IDX | 需求部门 |
| positionName | string | | 招聘岗位 |
| headcount | number | | 招聘人数 |
| urgency | string | IDX | high / medium / low |
| expectedDate | string YYYY-MM-DD | | 期望到岗日期 |
| reason | string | | 新增 / 替换 |
| jobDesc | string | | 岗位职责与要求 |
| budget | number | | 招聘预算（可选） |
| status | string | IDX | draft / approving / recruiting / closed |

#### `candidate` 候选人简历库
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| name | 必填 string | IDX | 姓名 |
| gender | string | | |
| phone | string | | |
| email | string | | |
| appliedPosition | string | | 应聘岗位（文本快照） |
| positionId | string | IDX | 关联岗位字典（可空，R-02） |
| source | string | IDX | 渠道（对应 sys_dict） |
| education | string | | 学历 |
| workYears | number | | 工作年限 |
| resumeAttachmentId | string | | 简历附件 |
| status | string | IDX | pending / screening / interviewing / hired / rejected / onboarded |
| requirementId | string | IDX | 关联招聘需求（可空） |
| employeeId | string | IDX | 入职建档后回填（E-05/S-05，可空） |

> 联动（E-05）：offer.status=accepted → 创建 employee（status=active，处于试用期则建 probation 记录）→ candidate.status=onboarded + 回填 candidate.employeeId + offer.employeeId，同事务完成。

#### `interview` 面试记录
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| candidateId | string | IDX | |
| positionName | string | | 应聘岗位快照（E-04，生成时固化，防候选人改岗后历史漂移） |
| round | number | | 轮次 1/2/3 |
| interviewer | string | | 面试官 |
| interviewDate | string YYYY-MM-DD | IDX | |
| method | string | | onsite / video / phone |
| comment | string | | 评价 |
| result | string | | pass / pending / fail |
| nextStep | string | | 下一轮安排 |

#### `offer` offer 发放记录
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| candidateId | string | IDX | |
| employeeId | string | IDX | 入职建档后回填（可空） |
| positionName | string | | 应聘岗位 |
| offerDate | string YYYY-MM-DD | | |
| salary | object | | {base, performance, allowance} |
| onboardDate | string YYYY-MM-DD | | 入职日期 |
| expireDate | string YYYY-MM-DD | | 有效期 |
| status | string | IDX | issued / accepted / rejected / expired |

### 3.4 考勤

#### `attendance_request` 请假/加班/出差申请（统一单）
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| type | string | IDX | leave / overtime / business_trip |
| leaveType | string | | 请假细分：annual / sick / personal / marriage / maternity / compensatory / other（type=leave 时） |
| startTime | string YYYY-MM-DD | | |
| endTime | string YYYY-MM-DD | | |
| duration | number | | 时长（leave/trip 按天，overtime 按小时，依 type 解释） |
| reason | string | | 事由 |
| attachmentId | string | | 附件（病假证明等） |
| submitTime | number (ms) | | 提交时间（E-06，草稿→待审批时写入） |
| status | string | IDX | draft / pending / approved / rejected |
| approvalChain | object | | 审批链（单级=[{approver}]，多级扩展见《需求单》§4.1） |
| approvalRecordIds | array | | 审批留痕 id 列表 |

> **审批状态枚举统一（S-03/F-02）**：所有审批类单据（attendance_request / attendance_remedy / salary_adjust / transfer_record）状态枚举统一为 `draft / pending / approved / rejected`，由 `sys_dict`(group=approval_status) 管理；仅可编辑 draft/rejected 状态单据。

#### `attendance_remedy` 补卡申请
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| remedyDate | string YYYY-MM-DD | IDX | 补卡日期 |
| timePoint | string | | on / off（上班/下班） |
| reason | string | | 原因 |
| status | string | IDX | draft / pending / approved / rejected |
| approvalRecordIds | array | | 审批留痕 id 列表 |

> 联动：补卡审批通过后，若同员工同日期存在对应 `attendance_anomaly`（缺卡/迟到），自动标记 handled=true 并写 handleNote 注明"补卡通过"。

#### `attendance_anomaly` 考勤异常
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| date | string YYYY-MM-DD | IDX | |
| type | string | IDX | late / early_leave / missing_punch / absenteeism / overtime_abnormal / duplicate |
| detail | string | | 异常明细 |
| handled | boolean | IDX | 是否已处理 |
| handleNote | string | | 处理备注 |

> **异常判定规则（E-07/S-06）**：判定依据 = 排班（schedule）+ 已通过申请单（attendance_request）+ 补卡（attendance_remedy）+ 休息日规则。**未排班日解释规则**：按 `hr.settings.attendance` 中部门默认班次或周末休息配置判定（未配置默认=休息，不产生缺卡）；有排班但无打卡记录=缺卡。判定在月度汇总生成时批量执行并写入 anomaly。

#### `shift` 班次字典
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| name | string | | 班次名称（早班/晚班/夜班） |
| startTime | string | | HH:mm |
| endTime | string | | HH:mm |
| flexible | boolean | | 是否弹性 |
| color | string | | 排班表显示色 |

#### `schedule` 排班明细
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| workDate | string YYYY-MM-DD | IDX | 日期 |
| shiftId | string | IDX | 班次 |
| departmentId | string | IDX | 冗余部门 id（R-06，支撑部门维度周/月视图） |

> 复合索引 (employeeId, workDate) 支撑员工周/月排班；部门视图用 (departmentId, workDate)。**未排班日语义（E-07）**：未排班=休息（按 §3.4 异常判定规则解释，不产生缺卡异常）。

#### `attendance_monthly` 月度考勤汇总（固化）
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| month | string | IDX | YYYY-MM |
| expectedDays | number | | 应出勤 |
| actualDays | number | | 实际出勤 |
| lateCount / earlyCount / absentDays | number | | 迟到/早退/缺勤 |
| leaveDays | object | | 各假种天数 {annual, sick, ...} |
| overtimeHours | number | | 加班时长 |
| tripDays | number | | 出差天数 |
| anomalyCount | number | | 异常次数 |
| manualAdjusted | boolean | | 是否含手工修正（E-08） |

> 由排班 + 申请单 + 异常自动汇总生成。**手工修正与重算（E-08）**：手工修正的单元格置 manualAdjusted=true（锁定），重新生成时跳过锁定单元格，其余按规则覆盖。

### 3.5 薪资

#### `salary_account` 员工薪资台账（版本化历史表，S-04）
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| base | number | | 基本工资 |
| position | number | | 岗位工资 |
| performance | number | | 绩效工资 |
| overtimeFixed | number | | 固定加班费（E-09） |
| allowances | object | | 津贴/补贴 {meal, transport, communication, other}（口径：津贴=固定发放，补贴=按次发放走浮动项） |
| effectiveFrom | string YYYY-MM-DD | IDX | 生效日期 |
| total | number | | 月薪合计（派生） |

> **版本化规则（S-04）**：同一员工允许多条记录按 effectiveFrom 排序；"当前薪资"= effectiveFrom ≤ 今天的最新一条。调薪生效 = 审批通过事务内插入新版本（由 salary_adjust 或 transfer_record 触发）；工资表生成 = 取生成时点快照（见 payroll_item）。adjustCount 由 salary_adjust + transfer_record(salary_change) 统计派生，不落冗余字段。

#### `payroll` 月度工资表（主表）
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| month | string | IDX | YYYY-MM |
| status | string | IDX | generated / confirmed / issued（记录存在即已生成） |
| generatedAt | number (ms) | | 生成时间 |
| totalGross / totalNet | number | | 应发/实发合计（派生汇总） |

#### `payroll_item` 工资表明细
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| payrollId | string | IDX | 所属工资表 |
| employeeId | string | IDX | |
| baseItems | object | | **快照**（F-07）：{base, position, performance, overtimeFixed, allowances}，**工资表生成时固化**，之后调薪不回溯 |
| floatingItems | object | | 浮动项 {performance, overtime, bonus, subsidy, deduction} |
| tax | number | | 个税 |
| socialPersonal / fundPersonal | number | | 社保/公积金个人部分 |
| otherDeductions | object | | 其他扣款 |
| gross / net | number | | 应发 / 实发 |

#### `salary_adjust` 薪资调整记录
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| type | string | | raise / grade_change |
| reason | string | | 调整原因 |
| oldSalary / newSalary | number | | 原/新薪资 |
| effectiveFrom | string YYYY-MM-DD | | 生效日期 |
| status | string | IDX | draft / pending / approved / rejected |
| approvalRecordIds | array | | 审批留痕 id 列表 |

> **与异动调薪的职责划分（D1，推荐方案 A）**：`salary_adjust` 仅记录纯调薪（无岗位/职级变动）；伴随调岗/晋升的调薪走 `transfer_record(salary_change)`；两者审批通过后统一插入 salary_account 新版本。UI"薪资调整记录"页聚合展示两者（标注来源类型）。

#### `deduction_detail` 个税/社保代扣明细
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| month | string | IDX | |
| employeeId | string | IDX | |
| taxableIncome | number | | 应纳税所得额 |
| tax | number | | 个税 |
| socialItems | object | | 社保个人 {pension, medical, unemployment, ...} |
| fundPersonal | number | | 公积金个人 |

#### `payslip_issue` 工资条发放记录（聚合视图）
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| month | string | IDX | |
| payrollId | string | | 来源工资表 |
| method | string | | print / email / offline |
| issuedAt | number (ms) | | 发放时间 |
| status | string | | not_issued / issued / partial |

#### `payslip_receipt` 工资条员工级发放明细（E-11）
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| issueId | string | IDX | 所属发放记录 |
| employeeId | string | IDX | |
| issued | boolean | | 是否已发放 |
| issuedAt | number (ms) | | 发放时间 |

> 部分发放时按 receipt 下钻"谁发了、谁没发"；issuedCount/totalCount 由 receipt 聚合派生，不再冗余存储。

### 3.6 绩效

#### `perf_cycle` 考核周期
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| name | string | | 周期名称 |
| type | string | IDX | monthly / quarterly / half_year / yearly |
| startDate / endDate | string YYYY-MM-DD | | |
| departmentIds | array | | 适用部门（空=全部） |
| status | string | IDX | not_started / running / finished |

#### `perf_review` 绩效考核表
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| cycleId | string | IDX | |
| employeeId | string | IDX | |
| selfScore / managerScore | number | | 自评/上级评分 |
| totalScore | number | | 总分 |
| grade | string | IDX | S / A / B / C / D |
| status | string | IDX | draft / submitted / confirmed |

#### `perf_review_item` 考核指标明细
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| reviewId | string | IDX | |
| indicator | string | | 指标名称 |
| weight | number | | 权重 % |
| target / actual | number | | 目标值/完成值（F-06，数值型，百分比类约定 0-100） |
| targetUnit | string | | 目标单位（元/件/次/%，可空） |
| score | number | | 评分 |

> 等级规则（E-12/S-03）：绩效等级 S/A/B/C/D 及分界由 `sys_dict`(group=perf_grade) 管理（存 {grade, label, min}），`perf_review.grade` 存字典 value，分数→等级映射在确认时按字典规则计算。

#### `perf_interview` 绩效面谈记录
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| cycleId | string | IDX | |
| interviewDate | string YYYY-MM-DD | | |
| interviewer | string | | 面谈人 |
| content | string | | 面谈内容 |
| improvementPlan | string | | 改进计划 |
| followUpDate | string YYYY-MM-DD | | 下次跟进日期 |

### 3.7 培训

#### `training_plan` 培训计划
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| title | string | | 培训主题 |
| type | string | IDX | onboarding / skill / management / safety / other |
| trainer | string | | 讲师（内部/外部） |
| startDate / endDate | string YYYY-MM-DD | IDX | |
| mode | string | | offline / online |
| target | string | | 对象（部门/岗位/名单） |
| budget | number | | 预算 |
| status | string | IDX | planning / ongoing / finished / cancelled |

#### `training_record` 培训记录归档
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| planId | string | IDX | 关联计划 |
| employeeIds | array | | 参训人员 |
| attendance | object | | 出勤情况 |
| materialAttachmentIds | array | | 资料附件 |
| finished | boolean | | 完成状态 |

#### `skill_cert` 技能证书
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| certName | string | | 证书名称 |
| certType | string | IDX | |
| issuer | string | | 发证机构 |
| certNo | string | | 证书编号 |
| issueDate | string YYYY-MM-DD | | 取得日期 |
| expireDate | string YYYY-MM-DD | IDX | 有效期（到期提醒） |
| attachmentId | string | | 证书附件 |

#### `training_eval` 培训效果评估
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| planId | string | IDX | |
| method | string | | questionnaire / exam / practice |
| traineeScore / trainerScore | number | | 参训人/讲师评分 |
| satisfaction | number | | 满意度 |
| conclusion | string | | 评估结论 |

### 3.8 合同

#### `contract` 合同台账（链式建模，E-13/S-08）
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| contractNo | string | | 合同编号（逻辑唯一，排除 deleted） |
| type | string | IDX | fixed(固定期限) / unlimited(无固定期限) / intern(实习) / labor_service(劳务) |
| startDate / endDate | string YYYY-MM-DD | IDX | 起止日期 |
| signDate | string YYYY-MM-DD | | 签订日期 |
| renewedFromId | string | IDX | 续签来源合同 id（首签为空，S-08） |
| renewCount | number | | 续签次数（派生，沿链计数） |
| changes | array | | 变更记录 [{date, type, reason}] |
| attachmentId | string | | 合同附件 |
| status | string | IDX | active(履行中) / expired(已到期) / renewed(已续签) / terminated(已终止·到期自然终止) / rescinded(已解除·提前解除) |

> **续签建模（E-13）**：续签 = 新建 contract 记录（renewedFromId 指向上一条）+ 原记录 status=renewed，形成合同链；合同到期提醒沿链按最新一条 endDate 计算。
> 合同状态中文语义逐值标注（F-04）：terminated=到期自然终止，rescinded=提前解除（可细分协商/裁员/违纪，见解除类型备注）。

#### `contract_template` 合同模板
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| name | string | | 模板名称 |
| type | string | IDX | 适用合同类型 |
| version | string | | 版本 |
| attachmentId | string | | 模板文件 |
| isDefault | boolean | | 默认模板 |

#### `probation` 试用期记录
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| startDate / endDate | string YYYY-MM-DD | IDX | 起止 |
| status | string | IDX | running / regularized / extended / early_regularized / failed |
| extendReason | string | | 延长原因（extended 时） |
| transferRecordId | string | | 关联转正异动记录 |

### 3.9 社保公积金

#### `social_insurance` 社保缴纳台账
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| month | string | IDX | YYYY-MM |
| employeeId | string | IDX | |
| base | number | | 缴费基数 |
| items | object | | {pension:{company,personal}, medical:{...}, unemployment, injury, maternity} |
| companyTotal / personalTotal | number | | 单位/个人合计 |

#### `housing_fund` 公积金缴纳台账
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| month | string | IDX | |
| employeeId | string | IDX | |
| base | number | | 基数 |
| companyRate / personalRate | number | | 比例 |
| companyAmount / personalAmount | number | | 金额 |

#### `social_declare` 增减员申报
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| type | string | | add / remove |
| insuranceTypes | array | | 险种 |
| month | string | IDX | 申报月份 |
| declareDate | string YYYY-MM-DD | | 申报日期 |
| status | string | IDX | pending / declared / done / rejected |
| remark | string | | 备注 |

#### `social_base_adjust` 缴费基数调整
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| type | string | | annual / individual |
| oldBase / newBase | number | | 原/新基数 |
| effectiveMonth | string | IDX | 生效月份 |
| status | string | IDX | pending / declared / done / rejected（E-14/F-03，与 social_declare 四态统一） |

#### `benefit_claim` 社保待遇申领
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| type | string | IDX | maternity / injury / medical / unemployment / other |
| amount | number | | 申领金额 |
| applyDate / receivedDate | string YYYY-MM-DD | | 申请/到账日期 |
| status | string | IDX | applying / received / rejected |

### 3.10 员工异动

#### `transfer_record` 员工异动统一记录
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| employeeId | string | IDX | |
| type | string | IDX | regularize / transfer / salary_change / promotion / demotion / resign |
| oldData | object | | 原 {departmentId, positionId, gradeId, salary} |
| newData | object | | 新值（同结构） |
| reason | string | | 原因 |
| evaluation | string | | 试用期表现评价（E-16，type=regularize 时必填） |
| resignType | string | | 离职类型 voluntary / involuntary / negotiated（E-17，type=resign 时必填） |
| effectiveDate | string YYYY-MM-DD | IDX | 生效日期 |
| status | string | IDX | draft / pending / approved / rejected |
| approvalRecordIds | array | | 审批留痕 |

> **联动规则与事务清单（R-10，事件 → 涉及表 → 单事务）**：
> - regularize 通过 → employee.regularDate 写入 + probation.status=regularized（单事务）
> - transfer/promotion/demotion 通过 → employee.departmentId/positionId/gradeId 更新（单事务）；salary_change 通过 → salary_account 插入新版本（单事务）
> - **resign 审批通过 → employee.status=resigning（E-18/S-07）→ resignation_handover.allDone=true 时 → status=resigned + resignInfo/resignDate 写入（单事务）**；花名册"离职"按钮=发起本审批（E-03）
> - offer.status=accepted → 创建 employee + candidate.status=onboarded + 回填 employeeId（单事务，见 §3.3 candidate 联动）
> - 部门撤销 → 该部门员工 departmentId 置空并提示重新分配 + headcount 停用 + 组织架构图更新（同一事务）
> - 异动审批通过/驳回 → 写 approval_record（与业务状态更新同事务）
> - 非审批类状态变更（合同 terminated、候选人 hired 等）→ 写 status_log（E-20，独立小事务）

#### `resignation_handover` 离职交接清单
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| transferRecordId | string | IDX | 关联离职异动 |
| workItems | array | | 工作交接 [{item, receiver, confirmed}] |
| propertyItems | array | | 物品归还 [{item, returned}] |
| accountItems | array | | 账号权限 [{account, reclaimed}] |
| allDone | boolean | | 全部完成标志 |

### 3.11 待办 / 备忘 / 常用网址

#### `todo` 每日待办
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| content | string | | 内容 |
| date | string YYYY-MM-DD | IDX | 日期 |
| priority | string | IDX | high / medium / low |
| category | string | | 分类标签 |
| done | boolean | IDX | 完成状态 |
| remindAt | number (ms) | | 提醒时间（可选） |

#### `memo` 备忘录笔记
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| title | string | | |
| content | string | | 富文本 HTML |
| category | string | IDX | |
| pinned | boolean | | 置顶 |
| createdAt / updatedAt | number (ms) | IDX | |

#### `reminder` 重要事项提醒
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| title | string | | 事项 |
| importance | string | | high / medium / low |
| remindAt | number (ms) | IDX | 提醒时间 |
| repeatRule | string | | none / daily / weekly / monthly |
| status | string | IDX | pending / reminded / done |

#### `quick_link` 常用网址
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| name | string | | |
| url | string | | |
| category | string | IDX | social_security / fund / tax / recruiting / oa / other |
| icon | string | | 图标（可选） |
| remark | string | | |
| sortOrder | number | | 排序 |

### 3.12 通用

#### `approval_record` 审批留痕
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| bizType | string | IDX | 单据类型（attendance_request / transfer_record / ...） |
| bizId | string | IDX | 单据 id |
| nodeIndex | number | | 节点序号（单级=0） |
| approver | string | | 审批人（当前=liqianyu） |
| action | string | | approve / reject / resubmit |
| comment | string | | 意见 |
| actedAt | number (ms) | | 操作时间 |

> **多级扩展预留**：`approval_record` 天然支持多节点（nodeIndex 递增）；单据的 `approvalChain` 字段存配置化节点（单级=[{approver:'liqianyu'}]），未来升级仅增加节点配置，表结构不变。

#### `status_log` 状态变更留痕（E-20，非审批类）
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| bizType | string | IDX | contract / candidate / recruit_requirement / perf_review / ... |
| bizId | string | IDX | 业务记录 id |
| from / to | string | | 原状态 / 新状态 |
| at | number (ms) | | 变更时间 |
| operator | string | | 操作人（当前=liqianyu） |

> 覆盖非审批类状态流转（合同 active→terminated、候选人 screening→hired、招聘需求状态流转、考核表 draft→submitted 等），审批类单据的流转记录在 approval_record，两者职责分离。

#### `attachment` 统一附件库
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| fileName | 必填 string | | 原始文件名 |
| mimeType | string | | |
| size | number | | 字节数 |
| blob | Blob | | 文件数据 |
| uploadedAt | number (ms) | | |

> 不设 ownerType/ownerId 冗余字段（R-11）：同一附件可能被多业务引用；物理清理时扫描各业务表 attachmentId 引用，无引用才删除（见 §1.7）。

#### `sys_dict` 业务字典
| 字段 | 类型 | 索引 | 说明 |
|---|---|---|---|
| id | string | PK | |
| group | string | IDX | 组：recruit_channel / leave_type / anomaly_type / cert_type / benefit_type / perf_grade / approval_status / resign_type / contract_type / education / ... |
| label / value | string | | 显示值 / 存储值 |
| sortOrder | number | | 排序 |
| builtin | boolean | | 是否内置（内置可禁不可删） |

> 绩效等级组 perf_grade（E-12）：{grade:'S', label:'卓越', min:90} 等，等级分界可配置；审批状态组 approval_status（S-03）：draft/pending/approved/rejected 供统一审批组件使用。

---

## 4. 实体关系总览（文字 ER）

```
department 1─n employee 1─n employee_attachment
position   1─n employee（岗位）      grade 1─n employee（职级）
employee   1─n attendance_request / remedy / anomaly / schedule / attendance_monthly
employee   1─n salary_account / payroll_item / deduction_detail / salary_adjust
employee   1─n perf_review / perf_interview / skill_cert
employee   1─n contract / probation / social_insurance / housing_fund / social_declare / benefit_claim
employee   1─n transfer_record ─1 resignation_handover
recruit_requirement 1─n candidate 1─n interview / offer（offer/candidate 可回填 employeeId）
training_plan 1─n training_record / training_eval
payslip_issue 1─n payslip_receipt
attachment  ← 被 employee_attachment / contract / skill_cert / 简历 / 模板 等引用（attachmentId）
approval_record ← 被 attendance_request / attendance_remedy / transfer_record / salary_adjust 引用（approvalRecordIds）
status_log  ← 独立记录非审批类状态流转（bizType + bizId）
```

---

## 5. 索引与查询设计要点（R-07/I-02：复合索引已写回 §3 表定义）

1. **主键**：全部 `id` 为主键（out-of-line key）。
2. **复合索引（已正式写入各表定义，此处为汇总清单）**：
   - `schedule`: (employeeId, workDate) 员工月排班、(departmentId, workDate) 部门月排班
   - `attendance_monthly`: (month, employeeId) 月度汇总
   - `payroll_item`: (payrollId, employeeId) 工资表导出
   - `social_insurance`: (month, employeeId)
   - `housing_fund`: (month, employeeId)
3. **软删除过滤（R-08）**：高频表（employee / contract / attendance_request / payroll / attendance_monthly）建 `(deleted, status)` 或 `(deleted, month)` 复合索引，查询先按 deleted=0 定位；低频表结果集内过滤。
4. **聚合统计**：首页看板、渠道统计、绩效分布等均为内存聚合（数据量 ≤ 万级，秒级响应）；若后续数据量增长，可引入按月聚合缓存表。
5. **唯一性校验（R-09）**：工号、合同编号、证书编号等唯一字段**不建 unique index**，写入前查询校验且排除 deleted=1 的记录 + UI 提示。

---

## 6. 数据访问层设计建议（开发约定）

- 封装 `db.js`（统一 API：`add / get / put / delete(软) / query(options)`），所有模块只调用 API，不直接操作 IDB。
- **事务（R-10）**：涉及多表联动的操作（异动审批通过、offer 建档、部门撤销、调薪生效）用**单事务**保证一致性，任一失败整体回滚（联动清单见 §3.10 transfer_record）；单表操作用独立小事务。
- 写后自动触发首页看板数据刷新（事件订阅，非轮询）。
- 导出：IndexedDB 数据 → ZIP 全量备份（data.json + attachments/ + config.json，见 §1.7）/ Excel(CSV) 分模块导出；导入：ZIP 恢复（校验版本） / Excel 批量建档。
- **单连接 + 多标签页（I-06）**：db.js 维护单连接；监听 `onversionchange` 提示关闭旧标签页刷新，避免升级期写冲突。

---

## 7. 数据库版本升级与迁移策略（I-04）

- 版本号在 `open()` 时递增，`onupgradeneeded` 中执行迁移脚本，按 `oldVersion → newVersion` 分步迁移，保证旧数据兼容。
- **IndexedDB 索引不可修改只能删建**：加/改索引必须在 onupgradeneeded 中 `deleteIndex('旧') → createIndex('新', ...)` 后重建，随新版本号提交。
- **逐版本迁移脚本注册表示例**：
  - `v1 → v2`：新增 store `grade`、`status_log`、`payslip_receipt`；`position` 表 deleteIndex/createIndex（复合索引重建）；`employee` 加字段（结构变更在 put 时补齐，读时兜底默认值）。
  - `v2 → v3`：字段/枚举扩展时，遍历目标 store 读取→转换→重写（如 status 枚举统一、date-only 字符串化）。
- `hr.data.version` 记录当前数据结构版本，导入恢复时校验版本一致性；每次升级前自动触发一次全量备份提示。

---

*本设计以《需求单-人事管理工作台 v1.1》为依据；如评审调整，随需求单同步修订。*
